To run this Scrapy project via scrapyd daemon:

1. Install scrapyd and scrapyd-deploy

2. Copy or symlink scrapyd.conf to /etc/scrapyd/scrapyd.conf

3. Run ./bin/addproject.sh

4. Configure remote or local accsess to the main database (to get active sites).
